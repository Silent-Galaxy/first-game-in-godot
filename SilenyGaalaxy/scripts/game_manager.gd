extends Node

# --- متغیرهای وضعیت بازی ---
var score = 0 # متغیری برای ذخیره تعداد سکه‌های جمع‌آوری شده

# --- دسترسی به رابط کاربری (UI) ---
# از @onready استفاده می‌کنیم تا مطمئن شویم وقتی بازی شروع شد و نودها لود شدند،
# این متغیر مقداردهی می‌شود.
# نکته: اگر لیبل را به عنوان Unique Name (%) ذخیره کرده باشید، اینجا %ScoreLabel استفاده می‌شود
@onready var score_label = $ScoreLabel 

# --- توابع مدیریت بازی ---

# این تابع زمانی صدا زده می‌شود که بازیکن یک سکه را بردارد
# (توسط اسکریپت سکه یا سیگنال فراخوانی می‌شود)
func add_point():
	# 1. افزایش امتیاز
	score += 1
	
	# 2. به‌روزرسانی متن روی صفحه
	# برای نمایش عدد در کنار متن، باید آن را به رشته (String) تبدیل کنیم
	score_label.text = "You collected " + str(score) + " coins."
	
	# چاپ در کنسول برای دیباگ کردن (اختیاری)
	print("Current Score: ", score)
